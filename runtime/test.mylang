type None;

type List<T>{
  head: T,
  tail: (List<T> | None),
};

// FIXME: A<int> isn't working because it's applying <int> to A, the GenericParameter, not to what the GenericParameter will be replaced with, which is List.
type GenericGeneric<A> {
  type: A<int>,
};

print(GenericGeneric<List>);
